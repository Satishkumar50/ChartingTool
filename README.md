Features section covering charts, data sources (with live price method per source), indicators, all 14 drawing tools, watchlist, alerts, workspaces
Quick Start — both the run.bat double-click method and the manual pip install / python app.py route
Project structure — every file listed with a one-line description
API reference — all 9 endpoints with the /api/history params table (including the end lazy-scroll param)
Adding a new data source — step-by-step guide so contributors know the pattern
Configuration — port override and Binance credentials note
Notes — data caching TTLs, the local-only SSL warning, and the yfinance coverage caveat
